# File Renamer — modern macOS (11+) build.
# The app itself needs nothing but the standard library; Pillow is for the
# icon and PyInstaller for the .app bundle.
Pillow>=10.0
pyinstaller>=6.10
# Optional: drag-and-drop onto the file list. The app runs without it.
tkinterdnd2>=0.4
